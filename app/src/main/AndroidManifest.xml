<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Package visibility (Android 11+ / API 30).

      We deliberately do NOT request QUERY_ALL_PACKAGES: Google Play requires a
      strong justification for it and rejects apps that ask for it without one.
      Matching only ever happens against the packages that exist in the deals
      catalog, so declaring exactly those packages here is both sufficient and
      the minimum amount of visibility the app needs.

      Keep this list in sync with app/src/main/assets/deals_catalog.json.
    -->
    <queries>
        <package android:name="com.spotify.music" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.netflix.mediaclient" />
        <package android:name="com.duolingo" />
        <package android:name="com.canva.editor" />
        <package android:name="notion.id" />
        <package android:name="com.lemon.lvoverseas" />
        <package android:name="com.nordvpn.android" />
        <package android:name="com.adobe.lrmobile" />
        <package android:name="com.picsart.studio" />
        <package android:name="com.getsomeheadspace.android" />
        <package android:name="ru.yandex.music" />
        <package android:name="ru.kinopoisk" />
        <package android:name="org.coursera.android" />
        <package android:name="com.strava" />
        <package android:name="org.telegram.messenger" />
        <package android:name="com.google.android.apps.subscriptions.red" />
        <package android:name="com.microsoft.office.officehubrow" />
        <package android:name="com.tinder" />
        <package android:name="com.grammarly.android.keyboard" />

        <!-- Needed to open store / deep links from a deal card. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".TrialTrackerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrialTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.TrialTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          WorkManager is initialised on demand from TrialTrackerApp so that the
          periodic catalog check only starts once the user has opted in.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge"
            xmlns:tools="http://schemas.android.com/tools">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
